[project]
name = "temichev-media-hub-contracts"
version = "0.1.0"
description = "Shared API and connector contracts"
requires-python = ">=3.13"
dependencies = ["pydantic>=2.11"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
