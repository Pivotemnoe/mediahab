[project]
name = "temichev-media-hub-connector-base"
version = "0.1.0"
description = "Base connector primitives"
requires-python = ">=3.13"
dependencies = ["pydantic>=2.11"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
