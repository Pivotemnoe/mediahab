# UI Phase 11J — отчёт

## Что сделано

- Исправлена десктопная версия Content Studio после обратной связи владельца: `Мастер материала` больше не выглядит второстепенной левой колонкой.
- На широком вебе основной экран теперь начинается с пользовательского пути: `Мастер материала`, `Сбор материала` и `Превью площадок`.
- Для 1440px используется две основные колонки, чтобы не было наложения карточек и сжатия рабочей зоны.
- Для 1920px добавляется третья колонка с превью площадок справа.
- Старая техническая студия перенесена в закрытый блок `Расширенный режим: факты, проверки, мастер-черновик и история`.
- Мобильная компоновка из UI Phase 11I сохранена без смены сценария.
- Visual smoke расширен проверками 390px, 1440px и 1920px, включая закрытый advanced-режим на десктопе.

## Проверки

- `make typecheck` — пройдено.
- `make lint` — пройдено.
- `/Users/konstantin/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm --filter @temichev/web build` — пройдено.
- `make test` — пройдено: UI hardening checks, 5 общих unittest, 43 API unittest.
- `tools/check_content_fixture_visual_smoke.mjs` — пройдено на 390px, 1440px и 1920px.
- `make validate-spec` — пройдено перед добавлением этого отчёта; после отчёта пакет спецификации нужно пересобрать повторно.

## Visual smoke

- 390px: мастер материала остаётся первым пользовательским путём, подробности стартуют закрытыми, кнопка Telegram остаётся внутри блока платформы.
- 1440px: мастер и сбор материала занимают основной desktop workspace, превью площадок вынесено отдельным блоком ниже, наложений нет.
- 1920px: мастер, сбор материала и превью площадок видны в одной строке как три рабочие зоны.
- На десктопе техническая студия есть в DOM и доступна в `Расширенный режим`, но стартует закрытой.

## Миграции

- Миграции не добавлялись.
- OpenAPI и typed client не менялись, потому что API-контракты не менялись.

## Ограничения

- В этом срезе изменена иерархия десктопного UI, а не полный визуальный редизайн всех старых технических карточек.
- `Расширенный режим` пока использует существующие карточки guided form, факт-локов, проверок, черновика, ИИ-предложений и истории.

## Следующий шаг

- Если владелец подтвердит новую десктопную иерархию, следующим срезом можно привести карточки внутри `Расширенного режима` к более компактному редакторскому виду.
